#lang scribble/base
@(require "preamble.rkt")

@title{Probably some math here}
@section{Section?}

@centered{@bold{God knowns, but at the same time he does not exist.}}

This is inline math: @($ "x^2 + y^2 = z^2").

Here is a displayed equation:

@($$ "\\sum_{i=0}^n x_i^3")
